import 'package:flutter/material.dart';

class LinkDetailScreen extends StatelessWidget {
  const LinkDetailScreen({Key? key}) : super(key: key);
  static const String id = '/link';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
