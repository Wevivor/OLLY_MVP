import 'package:flutter/material.dart';

class BoardDetailScreen extends StatelessWidget {
  const BoardDetailScreen({Key? key}) : super(key: key);
  static const String id = '/board/id';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
